# More apps

## ADDM

Dictionary tool

Want: extendibility. modularity. readability. mass editing of entries. schema for entries. export formats. recoverable file. type warnings if entry doesn't match the schema. receive user defined input. documentational database with XML structure. the schema can be chosen from a template and then edited

## Declensioner

Create declension tables or fill declension fields in dictionary

## Frequenta

It could report the frequencies of phonemes in a word input list

## Phonology table creator

Using features, and then setting the x axis features, and y axis features, input the phonemes and they get put in a nice table

## Scripta

Programatically create a font

```txt
grapheme = "a" {
   path {
      12, 31
      straight
      4, 11
      curved
      node = 8, 9
      serif = "1"
   }
}
```